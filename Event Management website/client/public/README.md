The repository already includes `client/images/codesky.png`.

The app uses `/images/codesky.png` by default. You can also copy your preferred logo into `client/public/logo.png` to serve it at `/logo.png`.

Recommended formats: PNG or SVG. Recommended size: around 600x600 for best results.
